<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <form class="" action="/songs/<%=@song.slug%>" method="post">
      <input type="hidden" name="_method" value="patch">
      <p><label for="song_name"><%=@song.name%></label>
      <input id="song_name" type="text" name="song[name]" value="<%@song.id%>"></p>
      <p><label for="Artist Name"><%=@song.artist.name%></label>
      <input id="Artist Name" type="text" name="song[artist_name]" value="<%@song.artist.id%>"></p>
      <h2>Genres:</h2>
      <% Genre.all.each do |genre| %>
      <p><label for="<%genre.name%>"><%=genre.name%></label>
      <input id="<%= genre.name %>" type="checkbox" name="song[genre_ids][]" value="<%= genre.id %>"></p>
      <% end %>
      <p>
      <input type="submit" name="" value="Save">

    </form>

  </body>
</html>
